// Code -> Element -> Item -> Component -> Fractals -> Unidirectional Flow
// Item -> List
//
// AssetInfo = item
// Landing Page = List <Stripe> / List <Item>
// On Demand = List <Stripe> / List <Item>
// Favourites = List <Stripe> / List <Item>
// Movies = List <Gop> / List <Item>
// Fullscreen = Component
const config = {
  classNames: {

  }
}

export default config
